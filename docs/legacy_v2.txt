Legacy System API - v2.0 (XML)  [NOTE: THIS DOC CONTAINS MISTAKES]

Base URL: http://localhost:4012

Auth (might be one of these):
- OAuth Bearer token in Authorization header
- Possibly 2FA
- Might require x-api-token (copied from v1 docs)

Content:
- JSON (this may be incorrect)

Endpoints:

1) Create Order
POST /orders   (MAY BE POST)
Body (JSON):
{
  "commodity_id": "string",
  "qty": number
}

2) Read Order
GET /orders/:order_id

3) Read Invoice
GET /invoices/:invoice_id

4) Post New Invoice
POST /invoices
Body:
{
  "amount": number
}

Note: Some systems may require first calling GET /order-template and then using it for create.
(unclear if needed; left ambiguous)